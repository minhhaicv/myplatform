<div class='row'>
    <h3 class="page-header">Menu</h3>
</div>

<div class="box-content">
    {% include 'menu/element/buttons.twg' with {'option': option} %}

    <form id='list-form' method='post'>
        <table class="table table-hover table-bordered ">
            <thead>
                <tr>
                    <th class='text-center list-option'>
                        <input class='checkall' type='checkbox' />
                        {{ include('menu/element/list-option.twg', {'checkall': 'true', 'form': 'list-form', 'delete_url': get('url', 'helper').generate("delete/" ~ option.branch) }) }}
                    </th>
                    <th>Title</th>
                    <th>Url</th>
                    <th>Last modified</th>
                </tr>
            </thead>
            <tbody>
            {% for item in data.list %}
                <tr>
                    <td class='text-center list-option'>
                        <input class='item' type='checkbox' name='{{ alias }}[{{ attribute(item, alias).id }}]' value='{{ attribute(item, alias).id }}' />
                        {{ include('menu/element/list-option.twg', {'item': item, 'edit_url' : get('url', 'helper').generate('edit/' ~ option.branch ~ '/' ~ attribute(item, alias).id)}) }}
                    </td>
                    <td>
                        <a href='' title='{{ attribute(item, alias).title }}'>
                        {{ attribute(item, alias).title }}
                        </a>
                    </td>
                    <td>
                        {{ attribute(item, alias).url }}
                    </td>
                    <td>
                        {{ attribute(item, alias).modified }}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </form>
</div>